name: websocket-chat

services:
  websocket-chat:
    build: ./
    restart: always
    volumes:
      - ../client:/usr/src/app/client
    networks:
        - network

networks:
  network:
      name: nginx-proxy-manager_default
      external: true

